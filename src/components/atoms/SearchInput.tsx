import React from "react";

const SearchInput = () => {
  return (
    <input
      className="form-control search-input"
      type="search"
      placeholder="Search"
      aria-label="Search"
    />
  );
};

export default SearchInput;
